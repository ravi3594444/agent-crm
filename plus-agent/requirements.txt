# Versiones FIJAS a propósito.
#
# Antes esto era todo `>=`. Hoy `langgraph>=0.2.60` resuelve a 1.2.x, que es
# un cambio de major: `create_react_agent` pasó de `state_modifier` a
# `prompt`. Un build de hace un mes y uno de hoy daban dos apps distintas, y
# la que fallaba lo hacía en el primer mensaje de un cliente.
#
# Estas son exactamente las versiones instaladas en el .venv con el que pasan
# los tests y con el que corre el agente desplegado.
# Para actualizar: subí UNA, corré `make test`, y recién entonces commiteá.

fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
redis==7.4.1
pydantic==2.13.5

langgraph==1.2.11
langgraph-checkpoint-redis==0.5.2
langchain==1.3.18
# Un solo proveedor: Qwen vía el endpoint OpenAI-compatible de DashScope
# (app/modelos.py). No hay paquete de Gemini ni de Anthropic a propósito: sin
# ellos no existe ningún "fallback" silencioso a otro modelo.
langchain-openai==1.6.0

# app/__init__.py hace load_dotenv() antes de que nadie lea os.environ.
python-dotenv==1.2.3

# La forma de un número, que NO es el idioma de la prosa: $12.000 y $12,000 son
# el mismo monto escrito para dos personas distintas, y `app/formato.py::pesos`
# daba vuelta los separadores a mano para una sola de ellas.
#
# NO es el módulo `locale` de la stdlib: `format_currency` toma el locale POR
# LLAMADA y no deja estado en el proceso, así que no hay `setlocale` global que
# se pise entre los hilos de barrido. Es la razón por la que se puede usar acá.
babel==2.18.0

# SÓLO para `MCP_EXTERNOS` (app/mcp_cliente.py): hablar con servidores MCP de
# terceros. El import vive adentro de `cargar()`, así que sin la variable puesta
# esto no se toca — pero SÍ se instala, y no es gratis: arrastra 12 paquetes
# (mcp, jsonschema, sse-starlette, pydantic-settings, PyJWT, attrs, …) a la
# imagen de producción. Ése es el precio de la superficie de un tercero, y está
# escrito acá para que se vea al leer el archivo y no al medir la imagen.
langchain-mcp-adapters==0.3.2
