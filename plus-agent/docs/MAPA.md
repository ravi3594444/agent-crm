# MAPA — qué hace cada archivo grande, para no volver a leerlo entero

Este archivo existe por una razón medible: `app/solicitudes.py` tiene 3011
líneas, `app/idioma.py` 2785, `app/limites.py` 1844 y `app/main.py` 1818. Una
sesión que empieza de cero y los lee enteros gasta la mitad de su contexto antes
de escribir una línea, y vuelve a descubrir las mismas trampas que descubrió la
sesión anterior.

**Cómo se usa:** leé la fila del módulo que vas a tocar y las trampas de abajo.
Andá al archivo sólo por lo que la fila no conteste.

**Cómo se mantiene:** es un acuerdo de trabajo en `CLAUDE.md` — el que cambia un
módulo actualiza su fila en el mismo commit. Un mapa desactualizado es peor que
no tenerlo, porque se le cree.

> Esto NO reemplaza a `CLAUDE.md` (las reglas duras), a `docs/AUDITORIA.md` (la
> deuda medida) ni a los docstrings de módulo, que siguen siendo donde vive el
> *porqué* largo. Esto es el índice.

---

## Los módulos, por tamaño

| archivo | líneas | qué es | lo que hay que saber antes de tocarlo |
|---|---:|---|---|
| `app/solicitudes.py` | 3056 | El almacén de **solicitudes de decisión**: un registro durable por pedido, escrito como comentarios `[solicitud] {json}` append-only donde **cada evento lleva la foto entera**. Redis es índice y caché, reconstruible desde ERPNext. | Leer es «el evento más nuevo que parsea gana», nunca un fold. `_escribir` devuelve False = *esto no pasó*. Dos conjuntos de estado: `ABIERTOS` (alguien debe contestar) y el más ancho `CON_PLAZO` (tiene plazo vivo); el índice y el barrido van por el segundo. Para preguntar «¿hay algo en curso?» la condición es `not in TERMINALES` y NUNCA un subconjunto: `ABIERTOS` deja `REVISION_HUMANA` afuera a propósito, así que filtrar por ahí rechaza los dos estados donde el cliente NO aceptó y permite el único donde sí. **Los límites del dueño corren cuando NADIE miró el pedido** (`limites_del_dueno`), **DESPUÉS de `_aplicar_terminos` y releyendo**: el cargo de la excepción sube el `grand_total`, así que mirar el documento de antes deja pasar el pedido que entra en el tope sin el cargo y se va por encima con él; y la fecha vieja —la que la oferta reemplaza— se leía como «fecha de entrega vencida». `entrega_acordada=True` apaga SÓLO el bloque de entrega de `policy`, porque un RETIRO se ofrece justamente cuando la dirección NO está en zona y si no se caería siempre. `lo_decidio_una_persona` exige un TELÉFONO, no «cualquier cosa que no sea `DECIDE_EL_SISTEMA`»: esa constante se imprime y se relee durante meses, y reescribirla haría que todos los registros viejos pasaran por humanos. Hay dos caminos que deciden solos —la excepción de entrega pre-autorizada y el respaldo de una solicitud vencida— y los dos terminaban en `submit_doc` sin haber pasado por `policy.evaluar`: sin tope, sin deuda vencida, sin cliente nuevo, sin cantidad por producto. `app/excepciones.py` ni siquiera importa `policy`, así que lo único que miraba era que la excepción estuviera activa y el mínimo; pasaba **incluso con `AUTO_CONFIRM_MAX=0`**. Lo que la regla del dueño autoriza es una FORMA de entrega, nunca la plata. `lo_decidio_una_persona` es la única definición de la diferencia (los dos caminos automáticos se marcaban distinto: uno dejaba `decidida_por` vacío, el otro escribía `DECIDE_EL_SISTEMA`). Con una persona detrás NO se vuelve a correr: ella miró ESTE pedido. No hace falta apagarle la regla de entrega a `policy` — el día de la semana no es suyo, `entrega.autorizada` mira la ZONA y `policy` no lee `ENTREGA_DIAS`. `soltar_reserva` re-lee DESPUÉS de escribir y devuelve `(probado, frase)`: es la única prueba de que un borrador dejó de tomar stock. Sus DOS éxitos no son el mismo hecho y hay constantes para distinguirlos: `LO_CERRO_ESTA_LLAMADA` es una transición que hizo esta llamada, `YA_ESTABA_CERRADO` es un borrador que cerró **cualquiera** — quien le atribuya el cierre a alguien tiene que mirar cuál de las dos vino, o termina escribiendo «lo dio de baja su cliente» sobre un rechazo del equipo. **`leer` colapsa una lectura fallida en `None`, o sea en «no hay solicitud»**; `leer_estricto` levanta `LecturaIncierta` y es lo que usa la única decisión irreversible (`decisiones.confirmar`): con ERPNext caído, «no pude leer» leído como «no hay contraoferta» emitía el pedido al precio viejo. Y hay una TERCERA, `leer_durable`, que se saltea la caché: las otras dos contestan con Redis cuando hay, y `_cachear` se traga los fallos a propósito, así que el rechazo o el vencimiento que otro worker dejó en ERPNext puede convivir con un `esperando_cliente` cacheado 30 días. `aceptar_cliente` la usa para la ÚLTIMA mirada antes del `submit_doc` —preguntarle ahí a la caché es preguntarle a la única copia que puede estar vieja por el mismo motivo por el que hay que volver a preguntar— y si esa lectura no contesta el cliente escucha «no pude mirarlo», nunca «no hay nada»: lo que sigue es el submit. `crear` corre **bajo `solicitud:{pedido}`** y **relee el borrador adentro** (`_sigue_en_borrador`, con la credencial del AGENTE, que este camino sale de una herramienta del modelo): el lock solo serializa, y sin la relectura el segundo en entrar igual le abre una solicitud a un pedido ya emitido. **Es el patrón que `agenda.py` generaliza — leelo antes de escribir cualquier cosa durable.** |
| `app/idioma.py` | 2785 | El catálogo ES/EN entero y `t(clave, idioma, **params)`. **Ya no es sólo del agente de clientes**: las cadenas fijas de las herramientas de gerencia (`crm`, `configuracion`, `gestion`, `captura`, `gerencia`) también salen de acá, que era el agujero por el que un dueño con el sistema en inglés recibía castellano en cuanto una herramienta contestaba algo que no fuera un número. | `t()` **nunca levanta**: una clave que no existe se loguea y se devuelve *tal cual*, así que un olvido viaja a WhatsApp como `pedido.algo` en vez de romper. `tests/test_idioma_cobertura.py` es lo que lo atrapa. Claves nuevas **al final del bloque**, que es como dos sesiones lo editan sin pisarse. Si las integrás a máquina, escribí el valor con `repr()` y no con un `.replace('"', '\\"')`: hay filas con un salto de línea adentro (`gestion.preparada` muestra la consecuencia en su propio renglón) y escaparle sólo las comillas deja el módulo partido en varias líneas, o sea un `SyntaxError`. Y si vas a reparar algo a máquina, acotá el reemplazo al bloque que insertaste: el archivo está lleno de concatenaciones implícitas (`ES: "…" "…"`) que a un parser de a líneas le parecen literales sin cerrar, y juntarlas mete `\n` literales adentro del texto — dos tests lo atraparon, y en otro lado hubiera sido texto roto en WhatsApp. |
| `app/limites.py` | 2307 | Todo lo que el dueño cambia sin entrar al servidor: cinco registros de `Definicion` (`LIMITES`, `ENTREGA`, `IDIOMAS`, `NEGOCIO`, `PLANTILLAS`) unidos en `TODOS`. | Un límite nuevo es **una entrada de dict y nada más** — alias, validación, código de cuatro dígitos y auditoría durable salen gratis. `maximo` **default 0.0**: si no lo declarás, todo valor menos 0 se rechaza. `opcional=True` es lo único que permite `NINGUNO`. `vigente()` **levanta** si Redis no está: nunca es una lectura segura. `idioma_gerencia_guardado()` tiene **TRES** respuestas y hay que distinguirlas: `None` = no se pudo leer, `""` = se leyó y no hay nada fijado, un idioma = lo que el dueño eligió con su código. Colapsar las dos primeras hace que un Redis caído se lea como «no fijó nada» y el entorno gane sobre una elección que sí existe. **Un registro nuevo necesita su propia marca durable**, y ésta es la trampa cara: `_auditar_en_erpnext` es un if/elif/**else**, y el `else` es `[limite]` — la marca que ARMA el fusible de `_almacen()`. Sin fila propia, cambiar el nombre de una plantilla deja escrita `[limite]`, y un flush de Redis posterior se lee como «se perdieron los límites del dueño»: `configuracion()` levanta para siempre y **ningún pedido se auto-confirma**, por haber tocado una plantilla. `NEGOCIO` y `PLANTILLAS` usan `[negocio]`, que no tiene lector ni fusible. **`validar()` no tiene `else`**: los seis `if` de tipo terminan en un `return _numero(...)` pelado, así que un `tipo` nuevo sin su rama no falla ruidosamente — le hace `float()` al nombre del negocio y contesta «no es un número». **`de_negocio()`/`varios_de_negocio()` NO son `vigente()`**: leen el mismo orden (almacén → `.env` → default) pero **no fallan cerrado**, porque ninguno de estos valores tiene una dirección peligrosa; que un Redis caído deje al cliente sin el aviso de que su pedido venció sí la tiene. Devuelven `""` para `NINGUNO`, así que los `if not plantilla:` que ya existían se leen igual. Un TEXTO que no valida se **recorta** en la lectura y **levanta** al teclearse: son dos preguntas distintas, y darles una sola respuesta hizo desaparecer el rubro del `.env` en vez de recortarlo. |
| `app/main.py` | 1818 | El webhook de Meta, el router determinista del equipo y los **cuatro hilos** de fondo. | El barrido es `_solicitudes_scheduler` (línea ~1566, cada 60 s): `solicitudes.tick()`, `pendientes.tick()`, `agenda.tick()`, **cada uno en su propio try/except**. El `from app import ...` está **fuera** del `while`: un error de import en un módulo nuevo mata el hilo entero en silencio. Códigos de 4 dígitos (ajustes) y 6 (acciones) se matchean **antes** del modelo y en orden. **Le pasa `inbound_message_id` a los DOS responders, y al de gerencia no se lo pasaba.** Sin él, `memoria._reclamar` no distingue otra vuelta del react loop de un mensaje nuevo, y su default para eso es preguntar de nuevo sin gastar el turno: la máquina de tres estados de la pregunta abierta no corrió NUNCA en producción, y el dueño recibió la misma pregunta en cada mensaje. El test del cable no lo vio porque se arma el `config` a mano, con la clave adentro: un test que fabrica el dato cuya presencia prueba no puede contradecir a producción sobre él. |
| `deploy/configurar_dashboard.py` | ~200 | Escribe los tokens del panel en el `.env`, atómicamente y en 0600. | **Dos modos y no son lo mismo**: sin `--persona` escribe el token COMPARTIDO, que mira y nunca decide (`puede_decidir` exige un nombre); con `--persona` agrega una entrada a `DASHBOARD_TOKENS`. Normaliza el teléfono con el `telefono.normalizar` del webhook, no con uno propio: guardarlo escrito de otra forma da un token que entra y no es nadie. Rechaza un número fuera de `TELEFONOS_EQUIPO` salvo `--solo-lectura`, y rechaza un segundo token para alguien que ya tiene uno. Corre **desde el host**, como `make seed`: `deploy/` no está adentro de la imagen. |
| `app/readiness.py` | 1387 | El preflight: valida `.env`, Meta, ERPNext, Redis, los límites y el PANEL, sin mostrar valores. | `chequear_panel` parsea con `dashboard.entradas_de_tokens`, el MISMO parser del panel, así que no puede aprobar una entrada que el panel después rechaza. El panel es opcional: «no hay ninguno» avisa, un panel MAL configurado bloquea. **`DASHBOARD_API_TOKEN` igual a una entrada de `DASHBOARD_TOKENS` es ERROR**: `quien()` recorre los por-persona primero, así que el token compartido pasaría a resolver a esa persona y con ella al derecho de confirmar. Lee el compartido con **`dashboard.normalizar_token`, la misma función** que usa `quien()` para comparar: con `_valor` los dos recortaban espacios pero eran dos reglas, y un `DASHBOARD_API_TOKEN=" … "` entrecomillado daba preflight en VERDE y 401 en todo. Ningún token se imprime, ni cortado. **`chequear_idioma` pregunta por lo GUARDADO (`limites.idioma_gerencia_guardado`) y no por `idioma.gerencia()`**: esa resolución cae a `os.environ`, que es el `.env` del proceso que corre el chequeo —o sea el VIEJO—, así que revisando un archivo candidato con `IDIOMA_GERENCIA=en` informaba ES. Un preflight que contesta sobre otro archivo es lo único que un preflight no puede hacer. **`PAIS_NEGOCIO` se valida contra los DATOS, no contra la forma**: `ZZ` tiene dos letras y es alfabético y pasaba en verde, mientras el runtime no derivaba nada y se caía al default argentino (+54, es_AR) sin una línea en ningún lado — se le pregunta a `pais.codigo_de` y `pais.locale_de`, que contestan "" cuando el territorio no existe. Y se le pregunta **sin escribir en `os.environ`**: la versión anterior lo pisaba y lo restauraba, que es una global de todo el proceso para responder algo que no la necesita. Misma familia que el idioma: los teléfonos del equipo se normalizan con **`telefono.normalizar(t, _codigo_pais(env))`**, porque `telefono` congela `PAIS`/`REGION` al importarse —o sea con el `.env` del proceso— y un preflight de un archivo candidato de otro país contestaba sobre duplicados y sobre el número del dueño mirando otra cosa. `chequear_panel` es la excepción y a propósito: ahí los dos lados se comparan entre sí y la otra mitad sale de `dashboard.entradas_de_tokens`, que usa el país del proceso. Un aviso de barrido que sale **fuera de la ventana de 24 h** va en `PLANTILLAS` *y* en `PLANTILLAS_BARRIDO_SIEMPRE` o `PLANTILLAS_BARRIDO_OPCIONAL`. Olvidarlo es un test rojo, a propósito. |
| `app/decisiones.py` | 1290 | Lo que el equipo decide sobre un pedido y el remito, y **la única puerta** para hacerlo. | `confirmar` ES la puerta: comprueba `es_equipo`, toma `confirmar:{pedido}`, bifurca si hay una solicitud abierta y cierra la revisión humana —igual que `cancelar`, `despreparar` y `_decidir`, que ya tenían esa forma—. Era un alias pelado de `aprobacion.confirmar_pedido` y las cuatro cosas vivían en `manejar_boton`, o sea en el camino de WhatsApp y en ninguna otra parte: un segundo llamador emitía el pedido AL PRECIO VIEJO mientras el cliente mira una contraoferta que no aceptó. `aprobacion.confirmar_pedido` sigue siendo el MECANISMO (toque repetido, submit que commitea tarde) y esto la POLÍTICA. **El lock no se llama `accion:` a propósito**: `acciones.py` envuelve `manejar_boton` con ese nombre y `locks.distributed_lock` usa `thread_local=False`, que no es reentrante — el orden es siempre `accion:` ⊃ `confirmar:` ⊃ `solicitud:`, uno solo y sin ciclos. **La comprobación y el submit van en la MISMA sección crítica, y el lock que la arma es `solicitud:{pedido}`**: `confirmar:` no lo toma nadie del lado del cliente, así que leer «no hay solicitud» bajo él no impedía que se abriera una un instante después (`solicitudes.crear` toma ahora el mismo lock). La lectura es `solicitud_abierta_estricta`: una lectura que no contesta **no emite nada**. `aprobar_solicitud` y `cerrar_revision_si_hay` se llaman **fuera del `with`** a propósito — las dos vuelven a tomar `solicitud:{pedido}` y el lock no es reentrante, así que desde adentro colgarían 10 s contra sí mismas y la rama de solicitud abierta dejaría de funcionar entera. **Adentro del `with` va `aprobacion.emitir` y NADA MÁS**, y el anuncio corre afuera: con la función entera adentro había once llamadas a ERPNext más un POST a Meta de 15 s —315 s de techo— bajo un lease de 60 que nadie renueva, así que el lease vencía en mitad de la sección, `solicitudes.crear` tomaba la llave libre y le abría una solicitud a un pedido que estaba por emitirse. La misma carrera, reabierta por el reloj. `LEASE_EMISION` es 180 porque la sección recortada son cuatro llamadas de 30 s; `LEASE_PUERTA` se queda en 60 a propósito, porque lo único que `confirmar:` serializa es confirmar contra confirmar y el anuncio es idempotente. **`confirmar` devuelve `emitido` además de `ok`, y no son lo mismo**: con una solicitud abierta la puerta aprueba la contraoferta —sale bien, `ok` es True— y NO emite nada; el pedido sigue borrador reservando stock. El panel leía `ok` y pintaba la fila como confirmada. `telefono_del_cliente(pedido)` es de acá. `_marcar_sin_reserva` **delega en `solicitudes.soltar_reserva`**: re-leía antes de escribir pero nunca después, así que «la escritura no levantó» se informaba como «el stock está libre» y el rastro durable del pedido lo afirmaba. Devuelve «liberado PROBADO», no «la llamada salió». |
| `app/consejos.py` | 1257 | **Lo único que el agente trae por su cuenta.** Cuatro detectores (perdimos plata, cliente dormido, deuda que envejece, quiebre antes del próximo reparto) y el presupuesto de cuántos se dicen por día. | **Python decide SI y a QUIÉN; el modelo sólo elige las palabras** — `cuerpo` es prosa determinista, ya usable si el modelo no está. **No manda nada**: lo manda el barrido de `main.py`, y `devolver()` es obligatorio cuando el aviso NO sale, porque `tick()` ya reclamó la clave con un `SET NX` y sin soltarla el consejo queda dicho sin que nadie lo haya oído. Todas las lecturas con `policy_get_*` (hilo de fondo = sin scope = credencial de CLIENTE). `_costo_de` filtra por `price_list`, `currency` Y `uom` y mide contra la fecha DEL PEDIDO, no contra hoy; una línea sin costo verificable hace que la pérdida se informe como PISO, y un pedido sin ninguna no produce consejo. El «compra seguido» es la MEDIANA de la historia de cada cliente, no una constante. **Tope de 3 por día y la razón está al lado**: desde el 1/10/2026 Meta cobra los mensajes de servicio por unidad. El reclamo vive SÓLO en Redis: un flush repite todo lo ya dicho. Arranca apagado (`CONSEJOS_ACTIVO=false`), y sin `CONSEJOS_LISTA_COSTO` el detector de pérdidas calla para siempre sin dar error. |
| `app/memoria.py` | 915 | **Los datos del negocio que el dueño repite todo el tiempo** («la panadería paga los viernes»), más la pregunta que el agente tiene pendiente de hacerle. | **Lo escribe una PERSONA, nunca el modelo por su cuenta**: esa distinción es todo el diseño, porque los modos de falla de las memorias de agente (hechos viejos, contradictorios, envenenados) salen todos de memoria escrita por el modelo. Un hecho DERIVADO («lo que suele pedir X») no va acá y no puede ir: envejece por construcción, es una consulta viva. Vive en un HASH pelado de Redis (`HGETALL plus-agent:memoria` los imprime) y NO en un doctype, entre otras cosas porque la credencial de CLIENTE tiene lectura ancha y esto es conocimiento comercial privado. El bloque del prompt está ACOTADO (20 datos / 2400 caracteres de 60 guardados) y cada línea va como cadena JSON, así que una nota no puede cerrar el bloque y abrir algo que parezca otra sección. Se elige por el más nuevo y se IMPRIME por clave: dos órdenes distintos a propósito, para que el prefijo del prompt no cambie en cada turno. `bloque_de_prompt` nunca levanta — con Redis caído el dueño sigue pudiendo preguntar cuánto vendió. **La frontera con el agente de clientes se movió y ya no es por FUNCIÓN sino por CLAVE**: `bloque_para_clientes` cruza sólo las claves marcadas `para_clientes=True` en `HUECOS` —las ocho preguntas de mostrador— y `prompt_clientes` lo inyecta ABAJO de las nueve reglas. Es una lista de lo PERMITIDO y no de lo prohibido, y ésa es la decisión: `anotar_dato` acepta cualquier `sobre=` que al dueño se le ocurra escribir por WhatsApp, así que una lista de claves vedadas sólo tapa lo que alguien previó. Las cuatro privadas (`cuenta_corriente`, `clientes_delicados`, `producto_clave`, `temporada`) y todo lo que nadie clasificó siguen sin cruzar — eso es lo que más se prueba, en `tests/test_memoria_cableado.py`, con almacén de verdad. `MEMORIA_PARA_CLIENTES=false` lo apaga entero, y un valor mal escrito también (un interruptor de privacidad falla hacia apagado). **Hay una SEGUNDA puerta**: `anotar(..., para_clientes=True)` —el dueño diciendo «esto contáselo a los clientes» sobre UNA nota, para lo que no es un hueco y nunca lo va a ser («esta semana la manteca está en promo»)—. Se guarda en el JSON del `Dato`, así que dura lo que dura la frase, y ausente se lee como False para que un cambio de formato no publique de golpe todo lo anotado hasta hoy. El piso es `NUNCA_PARA_CLIENTES` (las cuatro claves privadas) y se comprueba en los DOS lados: `anotar` rechaza la nota entera —no la guarda callado en privado, que dejaría al dueño creyendo que sus clientes se enteraron— y `bloque_para_clientes` vuelve a filtrar, porque una nota vieja o un `HSET` a mano no pasan por `anotar`. Y el dueño lo VE dos veces: en la confirmación del turno y marcado en el listado de `ver_memoria`, que es donde mira una semana después. Antes de marcar un hueco nuevo: si el dato ya es un ajuste de `limites.TODOS`, la nota lo va a contradecir y gana el ajuste. Los ocho de hoy no chocan con ninguno — `horario_corte` es hasta cuándo se puede PEDIR y `ENTREGA_HORA` es a qué hora LLEGA, y son dos hechos distintos. **Un `Hueco` tiene DOS formas de cerrarse y la diferencia no es cosmética.** Sin `ajuste`, la respuesta se guarda como NOTA bajo su `clave`. Con `ajuste="NOMBRE"`, la respuesta va por `proponer_limite` —con su código de cuatro dígitos— y el hueco se cierra cuando ESE ajuste tiene valor puesto **por el dueño** (`origen == "dueño"`, no «tiene un valor»: todos tienen el del `.env` o el default, y contarlos haría que nunca se pregunte por el nombre del negocio, que es justo lo que arranca sin configurar). La regla vieja prohibía preguntar por un ajuste; ahora se puede, y lo que impide la nota-que-miente es que **una nota NO cierra un hueco de ajuste** — hay un test que se muere si eso se colapsa. Los cuatro de ajuste entran en la MISMA rotación que los doce de nota: una pregunta por vez, la que hace más que no se pregunta. **La pregunta ABIERTA se re-comprueba contra el ajuste**, porque el dueño puede haberlo puesto desde el panel entre medio: sin eso el agente sigue preguntando por algo que ya está configurado. **Y DURA DOS TURNOS, NO VEINTE HORAS.** El bloque es una ORDEN —«preguntale ESTO»— y el modelo la obedece cada vez que la lee, así que con el turno abierto vivo veinte horas la misma pregunta salía en CADA mensaje hasta que la contestara: el dueño recibió «¿hay algún cliente al que convenga no dejarle acumular deuda?» seis veces seguidas, una por «hola». «No insistas» era una línea de prompt contra una orden de prompt, y perdía. Ahora el valor guardado lleva los dos turnos pegados (`clave|preguntado|escuchado`, un solo `SET NX` porque tres claves sueltas pueden quedar desparejas) y son tres estados: el turno que la reclamó la PREGUNTA; el siguiente NO la repite pero mantiene la clave, porque la respuesta llega justo ahí; el tercero la retira y abre `CLAVE_DESCANSO` (cuatro horas). Ignorarla compra silencio y no la siguiente del cuestionario —doce preguntas en doce mensajes es el mismo formulario con otra cara—; contestarla NO abre descanso, así que el que está contestando puede seguir. El descanso no alcanza a `ver_memoria` (`a_pedido=True`): el dueño que pregunta «¿qué más necesitás saber?» no se puede molestar con una respuesta que pidió, y contestarle «no me falta nada» con catorce huecos abiertos sería falso. El turno es `inbound_message_id` y lo pasa `prompt_gerencia`, que antes hacía `del config`: sin él las tres vueltas del react loop de un mensaje y los tres mensajes de una charla se leen igual, y ése era el cable roto. El desempate de la rotación es el ORDEN DE DECLARACIÓN y no la clave: recién instalado los dieciséis empatan, y el alfabeto ponía primera a `clientes_delicados` —una de las cuatro privadas— antes de que el agente supiera cómo se llama el negocio. Las doce plantillas de Meta quedan FUERA de la entrevista a propósito: `pedido_confirmado_v3` no es algo que alguien conteste hablando, se copia de la consola de Meta, y para eso está la pantalla de ajustes. |
| `app/acciones.py` | 1006 | Una orden en prosa del dueño convertida en UNA acción, confirmada con código de 6 dígitos. | Escribe `[accion]`, el único rastro de que una persona autorizó algo. |
| `app/policy.py` | 991 | `evaluar()`: la lista lineal de reglas de auto-confirmación. | **`ignorar_postura` tiene que llegar hasta `inventario.confiable`**: `_evaluar` aparta la postura y pone `inventario_habilitado = True`, pero después preguntaba a una función que volvía a mirar `maestra_encendida()` en su primera línea, así que con `STOCK_CONFIABLE=false` el modo sombra informaba «0 habrían pasado» pasara lo que pasara. **No se parte** — es el diseño, y partirla es cómo se pierde una regla. `_precio_autorizado` filtra Item Prices por `price_list`, `currency` **y** `uom`. |
| `app/precios.py` | 245 | **Cambiar el precio de LISTA de un producto, automáticamente.** Una función, `cambiar()`; `tools/crm.py::cambiar_precio` es sólo su puerta. | El dueño decidió que lo haga el modelo y sin confirmar cada vez, sabiendo que escribir la lista influye en lo que se auto-confirma (`policy._precio_estandar` compara el renglón contra ella). Lo que NO es preferencia suya y por eso está en Python: el modelo aporta **sólo el número** —`price_list` y `currency` salen de `policy`, la unidad del `stock_uom` leído de ERPNext, y a un Item Price al que le falte una de las tres no lo mira nadie y no da error—; el número tiene que caer adentro de `PRECIO_CAMBIO_MAX_PCT`; y hay **UN cambio por producto por día**, porque una banda por llamada no acota una SERIE y el modelo puede llamar cinco veces en un turno (15% × 5 = el doble). La reserva es `SET NX EX` ANTES de escribir, se suelta si el cambio se rechaza, y **falla cerrado** si Redis no contesta. Relee después de escribir con el MISMO filtro de `_precio_estandar`: «listo» por haber mandado el PUT contesta otra pregunta. Con la banda en 0 —el default— no escribe nada. La puerta genérica sigue cerrada: `Item Price` no está en `erpnext.DOCTYPES_EDITABLES`, así que sólo `erpnext.escribir_precio_de_lista` lo alcanza, y ésa tiene las tres columnas como argumentos obligatorios. **Tiene DOS puertas ahora** —el WhatsApp del dueño y `dashboard.cambiar_precio_desde_el_panel`— y una sola definición: la segunda sólo agrega de qué `canal` vino. **El rastro durable `[precio]` se escribe acá y DESPUÉS de releer**: anotar el PUT que se mandó, y no el precio que quedó, escribe historia sobre algo que puede no haber pasado. Un fallo del rastro NO deshace el cambio —a diferencia de `limites.aplicar`—: el precio ya está escrito y verificado, así que no aplicarlo no es una opción que exista; lo único que se puede hacer es decirlo por log. Antes de esto el único rastro era el `modified_by` del usuario de API de gerencia, que es el MISMO para el modelo y para el panel: o sea, la única escritura que mueve plata sin código no se podía auditar. `precio_actual()` devuelve **None y no 0** cuando no se pudo leer, y filtra por las mismas cuatro columnas que `cambiar`. |
| `app/pasos.py` | ~360 | **El presupuesto de UN turno, en llamadas al modelo, y qué se contesta cuando el turno no llega al final.** `correr()` envuelve las dos invocaciones del grafo; `cerrar()` es la respuesta honesta, y la usan los TRES cortes: el techo por excepción, el centinela, y **el proveedor que se cae a mitad de camino**. | El default de LangGraph 1.2.11 **no es el 25 de `langchain_core` sino 10007 superpasos**, o sea ~3335 llamadas al modelo por un mensaje de WhatsApp, y `graph._config()` no ponía `recursion_limit`: medido contra `responder_cliente`, **121 llamadas al modelo y 120 a herramientas**, y paró porque se acabó el guion. **LangGraph tiene DOS salidas y las dos estaban mal**: `recursion_limit = 3n` devuelve un `AIMessage` que dice `"Sorry, need more steps to process this request."` —no levanta, no se loguea, y esa frase en inglés sale tal cual por WhatsApp; `_non_empty` no la ataja porque no está vacía— y cualquier otro número levanta `GraphRecursionError`, que se volvía «tuve un problema técnico» tirando todo lo que el turno YA había averiguado. Se atajan las **dos**, y el centinela se compara por texto contra la fuente de la biblioteca. La conversión es `3n + 1` porque con `pre_model_hook` una vuelta gasta TRES superpasos: eso es una decisión de LangGraph, así que está **medida** en `tests/test_pasos.py` contra el grafo real y no leída en un README. **El cierre NO replica el hilo**: el turno se cortó justo después de pedir herramientas, así que la cola tiene un `AIMessage` con `tool_calls` sin su `ToolMessage` y mandarle eso a un proveedor compatible con OpenAI es un 400 en el peor momento; se saca TODO lo que sea llamada o respuesta de herramienta y lo averiguado viaja como un DATO aparte. Ojo con el doble: un `AIMessage` con `tool_calls` **y texto** es lo que manda un modelo de verdad, y con el texto vacío lo descartaba el guarda de «mensaje sin texto» dejando el de `tool_calls` sin probar —sacarlo dejaba los 17 tests en verde—. El cierre **recorta, y DESPUÉS de limpiar**: se saltea el `pre_model_hook`, así que sin recortar le mandaría el hilo entero —hasta 30 días— justo en la llamada que existe para acotar el gasto; y recortando ANTES gastaría el presupuesto de mensajes en los ToolMessages que igual no viajan, dejándolo con los datos y sin la pregunta (que es exactamente la cola que deja un turno que llegó al techo). El cierre **levanta** si el modelo no contesta: la disculpa de `_generate_response` es el piso y taparla sería inventarle una respuesta al cliente. Y queda **anotado en el hilo** con `update_state`, porque salió por el camino de excepción y el grafo sólo guarda lo que pasó por sus nodos: sin eso el cliente contesta «sí dale» a una oferta que el turno siguiente no ve. Si la anotación falla, la respuesta sale igual. **Por el camino del centinela se anota PISANDO** (mismo `id`, que es como reemplaza `add_messages`) y no debajo: ahí el grafo YA escribió su frase en inglés, y anotar debajo la deja en el checkpoint los 30 días del TTL —medido: el turno siguiente veía «Sorry, need more steps…» entre sus propios mensajes y lo leía como algo que había dicho él—. Lo que el techo NO deja es una llamada colgada: LangGraph mira el presupuesto arriba del bucle, así que el último superpaso completo es el de herramientas y los pares quedan cerrados; está medido en un test porque este techo es lo que vuelve alcanzable ese corte, que con el default de 10007 no se daba nunca. **El tercer corte es el que de verdad pasa**: un 429 del proveedor a mitad del turno tiraba lo averiguado y contestaba la disculpa técnica —medido: tres herramientas devolvían stock, precio y pedidos, y el cliente recibía «tuve un problema técnico»—, que es el MISMO defecto del techo con el disparador frecuente (la clave de Gemini en free tier es el problema #1 de `CLAUDE.md`). Y el cierre es la llamada con más chance de pasar justo ahí, porque va SIN herramientas: una llamada normal de gerencia manda ~29.100 tokens sólo de esquemas. Se cierra **sólo si hay un resultado de herramienta que contar**; sin hallazgos se relanza y queda exactamente el comportamiento anterior, porque una llamada más contra un proveedor que acaba de fallar es latencia que el cliente paga para escuchar la misma disculpa. Se loguea `el turno se cortó … type=…`: el TIPO ya se veía (`progreso.error_modelo` lo pone en la línea de latencia), lo que no se veía es si la persona recibió una respuesta o una disculpa. Los techos son `PASOS_MAX_CLIENTES`/`PASOS_MAX_GERENCIA` y **no** van por `app/limites.py`: no son una regla del negocio que el dueño cambie por WhatsApp, son cuánto aguanta el proceso. |
| `app/tools/pedidos.py` | ~1180 | Las herramientas de escritura del cliente: `crear_pedido`, `crear_cliente`, `escalar_a_humano`, `recordar`, `dar_de_baja_pedido`. | Los pedidos se crean en **un solo lugar** (`erpnext.create_doc("Sales Order", …)`). `_after_create` tiene **tres salidas** y `crear_pedido` **cuatro** que ni la tocan (idempotencia y recuperación): nada que se escriba ahí puede asumirse escrito una sola vez. **Definir el `@tool` no lo hace alcanzable**: el agente se arma de `TOOLS_CLIENTES` en `app/graph.py` al importar, así que una herramienta sin registrar se contesta con «esa herramienta no existe» y TODOS sus tests de comportamiento pueden estar verdes igual. «No encontré el pedido» es UNA sola frase para «no existe» y «no es tuyo», construida en un solo `return`: dos tokens distintos dejan enumerar pedidos ajenos. |
| `app/notificar.py` | 808 | El embudo único de todo lo que lee una persona del equipo, y el único lugar donde se resuelve **cómo se llama** una plantilla de Meta (`plantilla_vigente`, `idioma_de_plantilla`). | `avisar_dueno` avisa un fallo con un **retorno falso**, no con excepción. `NOTIFICAR_SOLO_PRIMERO` manda al **primer teléfono alfabético**, que puede no ser el dueño: para el dueño, `avisar_dueno`. Cuerpo capado a 1024 por Meta. `notificar_confirmacion(..., ventana=False)` cambia la ÚLTIMA línea del aviso: la anulación por WhatsApp la abre la marca durable de `confirmacion.registrar`, que falla sola —el pedido queda confirmado igual—, y prometerla siempre le decía al encargado que podía anular algo que el sistema iba a rechazar. `plantilla_vigente(variable)` reemplaza a los seis `os.getenv(...)` sueltos: el nombre lo fija el dueño desde el panel y el `.env` queda como valor de arranque. Su `except KeyError` **no es por las dudas** — `enviar_saliente` saca `plantilla_env` de una cola DURABLE, así que puede traer el nombre anterior a un renombre, y un aviso encolado no se pierde porque cambió el registro. |
| `app/autonomia.py` | 673 | Cuánto se confirma solo y qué lo frena. | Lee `pendientes._zona()` (un privado). |
| `app/dashboard_ui/app.js` | 1033 | El panel en JS sin framework: Today, Overview, Sales, Advice, Coming up, pedidos, inventario, clientes, agentes y conexión. | `snapshot` es el único fetch CRM del timer. Los controles tienen su propia fecha de lectura: refrescar operaciones no rejuvenece límites viejos. `loadViewReads` carga por pantalla y `loadRead` guarda por separado datos, error y fecha de lectura, y comparte la promesa de una lectura ya iniciada; un refresh del snapshot conserva esas lecturas. Today, queue y conversación tienen **validadores propios**, con los contratos de `docs/prompt-panel.md`; sus endpoints todavía los implementa otra rama. La conversación se abre **desde un cliente** (también uno de Today que quedó fuera del snapshot), nunca desde una lista de hashes. Sólo renderiza `customer`, `agent` y `note`, con texto escapado; no usa teléfonos, prompts ni argumentos de herramientas. `cerrarSesion` limpia `data` **y** `state.reads`; todo 401 autenticado llega ahí. Sesión + `detailRequest` impiden que una respuesta tardía reponga datos o pise otro diálogo. `makeDemo` cubre todas las pantallas y los estados sin número, sin mensajes y truncado. Los `what` de la agenda son frases del servidor; no reconstruirlas desde `type`. Sales y Advice agregan **sólo lecturas**, con proyección campo por campo en `validateSales`/`validateAdvice` y fixtures en `makeDemo(range)`. `/sales?days=7\|30` es la convención de período para el contrato pendiente; `/advice` no lleva período. Un `null` en Sales se muestra como no disponible, nunca cero; los topes de sus listas no implican un total incompleto. El cambio de período reemplaza el objeto de lectura: sesión + identidad del objeto impiden que respuestas anteriores lo pisen, incluso 7→30→7. El timer conserva estos informes y el refresh manual relee sólo el visible. Advice ordena por `weight`, no muestra porcentajes, explica `assumption` y conserva la vista previa apagada sin inventar hallazgos en vivo. Clientes fuera del snapshot se resuelven desde el ranking o la referencia del consejo antes de abrir el diálogo existente. Logo SVG y revelado decorativo de 1,4 s; tema claro por defecto y carbón optativo en `localStorage`, sin escrituras al servicio. **La PRIMERA escritura de esta pantalla es `apiWrite`**, y no es `apiRead` con un método distinto: un POST con cuerpo manda `Content-Type`, que NO está en la lista segura de CORS —el servidor lo permite, y si no lo hiciera el navegador rechazaría el preflight y no saldría ningún pedido, sin log en ninguna parte—. Usa el `{"error": ...}` del servidor cuando viene: taparlo con una frase genérica deja al dueño adivinando. **El formulario de un ajuste vive en un `<dialog>` y eso es obligatorio, no estético**: `render()` reemplaza `#app` entero y sólo le devuelve el foco y el cursor a `#search`, así que un input en la página pierde foco y texto en cada render —y el refresco de 60 s dispara uno—. El diálogo no lo toca `render()`. El ajuste que se está editando viaja en un input OCULTO y no en `state`: así no hay nada que resetear en `goto`, en `cerrarSesion` ni en el botón de demo, que son los tres lugares donde un estado de pantalla se olvida y reaparece donde no va. **200 con `ok:false` es el caso NORMAL** de un valor que no sirve: el servidor contesta la prosa que explica por qué y no cambia nada. Tratar todo 200 como éxito cerraba el diálogo diciendo que el cambio quedó pedido. El diálogo NO pide el código de cuatro dígitos y no existe pantalla que lo aplique: eso pasa por WhatsApp, a propósito. El precio de lista se cruza por `id` contra `/prices` y tiene **TRES** estados, no dos: no se leyó (`—`), se leyó y el producto no está en la lista de auto-confirmación (`Not priced`), y el precio. El del medio importa — en blanco, el dueño lee un problema de conexión sobre un producto al que sólo le falta el precio. Cuando no se puede cambiar un precio, la pantalla dice POR QUÉ en vez de no tener botón: la banda arranca en 0 y eso es la postura de fábrica, no una falla. |
| `app/dashboard_ui/styles.css` | 145 | Estilos del panel, compartidos por el agente y el export estático. | Las filas sin pedido tienen texto «No order yet», no sólo color. Coming up y los fallos de entrega siguen visibles en móvil. Los mensajes conservan saltos de línea y parten texto largo; no insertar HTML del transcript. Sales tiene métricas, gráfico navegable por teclado y rankings; Advice usa cuatro clases semánticas, supuestos visibles y estados vacíos. Manrope se conserva. El carbón es manual con `data-theme`, nunca por preferencia del sistema; incluye diálogos. El revelado no captura entrada y `prefers-reduced-motion` lo oculta. **El modo oscuro va por listas ENUMERADAS (`:is(...)`)**: una clase nueva no hereda nada y se queda con el color claro sobre fondo oscuro hasta que se la nombra. Y los tokens son `--border`/`--muted`/`--signal-red`: `--line` y `--danger` no existen, así que un `var()` mal escrito no falla, simplemente no pinta. |
| `../scripts/test-dashboard.mjs` | 1243 | Interacciones del panel en un DOM mínimo, sin navegador ni red. | Los dobles HTTP seleccionan la respuesta por **la URL recibida** y comprueban el bearer; no contestan una foto constante a cualquier endpoint. Cubre lecturas por vista, aislamiento al cerrar sesión/cambiar diálogo, validadores, escaping, retención, demo y topes junto a las listas. No es una prueba de layout ni de los endpoints nuevos. Las mutaciones dirigidas y sus resultados van en el commit. **42 tests, 18 nuevos**: fixtures Sales/Advice independientes del snapshot; cambio de período, carreras, 401, reintentos, proyección/escaping, moneda propia y conversión, apagado, filtros, links fuera del snapshot, tema y limpieza del logo. Cada test nuevo tiene una mutación que mata sólo ese test; consumidores compartidos se mutan por separado. Los resultados exactos van en el commit. **El harness tiene TRES listas escritas a mano y las tres se olvidan igual**: los ids que resuelve `querySelector`, los diálogos que devuelve `querySelectorAll('dialog')` y el doble de `fetch`. Un id que falte devuelve `null` y el primer render que lo lea se cae sin decir por qué; un diálogo que falte existe en `index.html` y no se cierra nunca acá. **`apiFixture` afirma `options.body === undefined`**, que es correcto para una lectura y hace imposible probar un POST: las escrituras llevan su propio doble (`writeFixture`). |
| `app/marcas.py` | 758 | **El registro tipado de los 16 marcadores durables** y el único lector/escritor. | Toda marca nueva es una fila de `_FILAS`: el `order_by` **se deriva** de `lectura` y el parser de `texto`, así que no se pueden separar. `porque_el_techo` es obligatorio. Agregar una fila rompe **3 tests escritos a mano** — actualizarlos a mano es el punto, generarlos los mata. Los tres son: la tabla `TEXTOS_DURABLES` y el censo `== 14` de `tests/test_marcas.py`, y el censo `== 16` de `tests/test_idioma_cobertura.py` — cuyo `set(...) == {...}` SALE del registro y por eso no se toca. `escribir` **no** deduplica (es un `add_comment` pelado): lo exactamente-una-vez lo pone el que llama. |
| `app/pendientes.py` | ~620 | El barrido de borradores esperando a una persona: sombra, recordatorio y cierre. | `en_silencio()` es la **única** implementación de las horas de silencio (22:00–07:00, ventana que cruza la medianoche). **La guarda nocturna tapa `_avisar` y NO `_cerrar`**: soltar una reserva no le habla a nadie, así que no espera — taparlo dejaba el stock tomado hasta las 07:00. `_tiene_marca` tiene **tres** respuestas y hay que comparar con `is not False`. El cierre delega en `agenda.ejecutar_ahora`. Todo apagado por default (`NINGUNO`). |
| `app/erpnext.py` | 604 | El cliente REST. **Tres identidades**: cliente, gerencia y política. | Un hilo de fondo **no tiene scope** y cae en la clave de *cliente*: por eso todo lector de fondo llama `policy_get_*` explícitamente. Un 404 y una caída son **la misma excepción**. `_list` levanta en vez de devolver `[]`. `get_list` **default limit=20**. **El cuerpo de la respuesta no va al modelo, pero ahora SÍ va al log.** Frappe no contesta un 4xx con una frase: manda `_server_messages`, un JSON con una lista de JSONs adentro, cada uno con su `message` en HTML. Lo que veía el que estaba mirando era «(estado 417)» y nada más, así que dos veces hubo que entrar al contenedor con `bench` a reproducir el documento para leer la validación — una de ellas, semanas después de que el stock dejara de cargarse. `_motivo_del_servidor` lo aplana y lo imprime; queda además en `ERPNextError.motivo`, que NO está en `str(exc)` a propósito: todo lo que se interpola en una respuesta de herramienta lo lee el modelo, y ese cuerpo puede traer datos de otros clientes. |
| `app/agenda.py` | ~1700 | **La lista durable de cosas que vencen más tarde.** Una fila = `(id, sobre, tipo, vence, estado, params)`; un barrido; handlers de ~10 líneas. | Los cinco conceptos —exactamente-una-vez, horas de silencio, re-leer, fallar cerrado, lote acotado— viven acá y **sólo** acá. Un comportamiento nuevo es una fila, no un módulo. `sobre` es **siempre** un Sales Order. **Un hecho con dos consumidores son DOS filas**: la baja que pide un cliente suelta la reserva (`baja_de_pedido`, fuera de `_HABLAN_CON_ALGUIEN`, corre a las 3 AM) y recién con la reserva PROBADA crea la fila que le habla al dueño (`aviso_baja_al_dueno`, adentro, espera a las 07:00). Una sola fila tendría que elegir, y las dos respuestas son correctas para su mitad. Un handler que devuelve `Resultado` termina la fila; `None` la deja viva y se reintenta; `reprogramar`+`params` es lo único que persiste estado entre intentos — y es lo que hace reanudable un efecto irreversible: `baja_de_pedido` guarda `fase="soltada"` apenas suelta la reserva, así que la marca y el aviso se reintentan SIN volver a cerrar nada y la fila no termina hasta que los dos quedaron durables. **El único orden de locks anidados del repo vive acá**: `_despachar` toma `pendiente:{sobre}` o `agenda:{sobre}`, y `baja_de_pedido` pide `solicitud:{sobre}` ADENTRO — porque re-leer la solicitud sin ese lock sólo achica la ventana en la que una aceptación hace Submit sobre el pedido que se está cerrando. No se traba porque va en una sola dirección: `pendiente:` se toma sólo en este módulo y ninguna función que tome `solicitud:` llega a despachar una fila. **Si alguna vez una lo hace, esto se abraza.** **Nada que venga después de un efecto irreversible puede ser «best effort» si la fila termina igual**: el reintento ya no puede distinguirse de una baja nueva — y cada evento gasta del techo 80 de `[agenda]`, así que un reintento por minuto se come la historia del pedido. **«¿Sigue vivo?» son DOS preguntas, no una**: `docstatus` 1/2, *y* un borrador cerrado (`docstatus=0` + `status` en `ESTADOS_SIN_RESERVA`, que incluye «On Hold»). `por_que_ya_no_vive(doc)` es la única definición y todo handler pasa por ella; mirar sólo `docstatus` fue un bug real que le avisaba a un cliente la hora de entrega de un pedido rechazado. **Un efecto y el mensaje que lo cuenta son DOS filas** también en el cierre: `cierre_borrador` suelta la reserva y corre a las 3 de la mañana, `aviso_cierre` habla y espera a las 07:00 — sólo el segundo está en `_HABLAN_CON_ALGUIEN`. `ejecutar_ahora` despacha también la fila que su fila dejó atrás, **la que dice `_HIJAS` y ninguna otra, y por id**; sin eso barría toda fila vencida del pedido, fuera de turno y fuera de `POR_RONDA`. La fila `aviso_cierre` vence en **0.0** para que su id sea estable: así `_asegurar_aviso_de_cierre` reintenta el `crear` sin dejar una gemela, que serían dos mensajes al mismo cliente. **Un handler sólo pone `ya_paso` si hizo algo irreversible** — `_aviso_cierre` no lo pone, porque lo único que aporta es el mensaje. Una fila que se reprograma compara **su propio** momento con `ahora`, nunca el plazo del que se deriva. `_cachear` es un **compare-and-set en Lua**: comparación, índice y blob en un script, con desempate por `sello` —y con sellos IGUALES lo terminal le gana a lo pendiente, porque una fila creada y despachada en el mismo acto tiene sus dos eventos con el mismo sello—. El doble de `tests/fakes.py` **replica** ese script en Python y por eso no puede discrepar con él: lo que lo prueba es `tests/test_agenda_redis.py`, contra un Redis de verdad. |
| `app/avisos.py` | 501 | La cola durable de salida al **cliente** (ZSET, score = cuándo vence). | `encolar` es **fail-closed**: si Redis no toma la escritura, **levanta**; no devuelve False. `False` significa *ya lo cubrió otro*. `encolar_equipo` usa la misma escala: True = al menos un destinatario **cubierto** (encolado ahora o ya encolado antes), False = *nadie se enteró y nadie se va a enterar*. Contar sólo lo recién escrito hacía que todo reintento reportara fracaso. Deduplica por `(evento, pedido)` **30 días**: dos filas del mismo tipo sobre el mismo pedido necesitan el id en el evento. Sus **dos scripts Lua** —el encolado idempotente y el reclamo con lease— los prueba `tests/test_scripts_redis.py` contra un Redis **de verdad**: `tests/fakes.py` los REIMPLEMENTA en Python, y una réplica no puede discrepar con su original (mutar `_RECLAMAR_LUA` mataba cero tests). |
| `app/locks.py` | ~135 | Los locks de negocio entre workers, y **de quién es el lease**. `distributed_lock` lo toma y falla cerrado; `Lease.sigue_mio()` contesta si todavía es tuyo. | Un lease **vence solo y nadie lo renueva** —no hay un `extend()` en el repo—, y adentro de las secciones críticas hay hasta cuatro llamadas a ERPNext con `timeout` de 20 y 30 s. Cuando vence, la llamada NO falla: sigue caminando hacia el Submit **sin exclusión mutua** mientras otro worker toma la llave libre. Hasta que existió `Lease`, `distributed_lock` hacía `yield None` y la pregunta «¿lo sigo teniendo?» era **imposible de hacer**: los dos lugares que emiten lo decían en un comentario y se conformaban con mirar si alguien había decidido mientras tanto, que achica la ventana y no la cierra. `sigue_mio()` **falla cerrado** —si Redis no contesta, la respuesta es que no— porque del otro lado hay el único Submit del sistema y «no pude comprobar» no es «sí». Expone UNA pregunta y **no** el `Lock` de redis-py: un llamador con `release()` podría soltar el lease de otro, y con `extend()` estirarlo para siempre. Los llamadores que no emiten siguen escribiendo `with distributed_lock(...):` sin `as` y no cambiaron. |
| `app/aprobacion.py` | ~345 | El toque de un botón de WhatsApp -> una acción real. **El pasillo, no la puerta.** | `manejar_boton` comprueba `es_equipo` y despacha; la rama `ok` es UNA LÍNEA a `decisiones.confirmar` y no decide nada por su cuenta. `confirmar_pedido` es el mecanismo probado contra el toque repetido y contra un submit que commitea después de que el cliente HTTP se dio por vencido; no comprueba quién llama y no debe: eso es de `decisiones.confirmar`. **Está partido en dos mitades con dueños distintos y la división es el contrato**: `emitir` (leer, comprobar el estado, Submit) es irreversible y va ADENTRO del lock del pedido; `anunciar` (el comentario, la marca durable, el aviso al equipo y el aviso al cliente) va AFUERA, y no es una optimización — adentro hay un POST a Meta de 15 s y el repo escribió tres veces que el teléfono de nadie va adentro de un lock. Es seguro afuera porque ya tenía que ser idempotente para aguantar el botón tocado dos veces. `Emision` es el traspaso entre las dos: `rechazo` no es None cuando la primera mitad terminó sin emitir, y `incierto` distingue un estado LEÍDO («está cancelado», que es un «no» firme) de una lectura que no contestó (que no es un «no», es un «no sé» — el Submit pudo commitear igual). **`anunciar` no puede levantar**: para cuando empieza, el Submit ya commiteó, así que una excepción ahí sería «falló la confirmación» sobre una venta cerrada. Cada pieza ya absorbe su fallo; el `except Exception` es el PISO para lo que ninguna prometió, y contesta `ok` True diciendo que los avisos no salieron. `confirmar_pedido` las compone para el que NO tiene el lock; `decisiones.confirmar` las llama por separado. `solicitud_abierta` no tiene guión bajo porque la usa `decisiones`; `acciones.py` tiene todavía su propia copia del mismo predicado. **Hay dos posturas sobre el mismo predicado y no son intercambiables**: `solicitud_abierta_estricta` levanta si no pudo leer y la usa `decisiones.confirmar`, que emite; `solicitud_abierta` atrapa y devuelve `None` porque sólo ENRUTA (el «no» del botón deriva a rechazar, que no emite nada). **`canal` no tiene default**: el comentario y la marca durable que quedan firmados en ERPNext decían «mediante WhatsApp» como literal, así que el primer camino que no fuera WhatsApp firmaba un canal por el que nadie pasó. El aviso al encargado NO lleva canal: manda la clave `gerencia.fuente_manual`, que se traduce y sigue siendo cierta desde cualquier puerta. |
| `app/dashboard.py` | 2034 | **El panel de administración**: una superficie ASGI aparte, con su propio bearer y su propio CORS, montada en `/api/dashboard`. Lectura, más **tres** escrituras. | `POST /orders/{id}/confirm`, `POST /settings/propose` y `POST /products/{id}/price` son las únicas rutas que no son GET; todo lo demás sigue contestando 405, y la excepción es el PAR (ruta, método): un GET a `/confirm` también es 405. **Una ruta de escritura nueva se nombra en DOS lados y olvidar el segundo es peor que olvidar el primero**: en la rama del método (si no, no anda nunca) y en la guarda del 403 (si no, anda DE MÁS — queda alcanzable por el token compartido, que es el de sólo lectura). **`POST /products/{id}/price` es la única escritura que mueve plata SIN código de cuatro dígitos** —el dueño lo pidió así— y lo único que la acota es `PRECIO_CAMBIO_MAX_PCT`, que arranca en 0, más un cambio por producto por día. **`POST /settings/propose` no cambia ningún ajuste**: deja UNO esperando el código de cuatro dígitos, que sale por WhatsApp desde `ajustes.preparar` —la única implementación, de la que ésta es la tercera llamadora—. **No hay ninguna ruta que aplique el código, y es el diseño**: el dueño confirma desde WhatsApp, o sea en otro aparato y por otro canal, que es lo que la regla dura 3 pide de verdad; y `limites.aplicar` no tiene contador de intentos porque no lo necesita mientras su única puerta sea un webhook firmado — publicarla por HTTP convertía 9000 valores con diez minutos de vida en fuerza bruta de segundos. **Nunca se devuelve el dict de `limites.proponer`**: trae el `codigo` adentro. Lo que viaja es prosa, y el test lo afirma sobre el cuerpo serializado ENTERO, porque `"codigo" not in body` sobrevive al valor anidado bajo otra clave. El cuerpo del POST se lee con `cuerpo_json`, **después** de las guardas (antes, un desconocido hace que el proceso buffelee megabytes) y **antes** del pool (adentro ya no hay loop para esperar `receive`); el techo de 8 KiB se mide ADENTRO del bucle, porque un `chunked` no manda `content-length` y comprobarlo al final es comprobarlo cuando ya se juntó todo — las dos versiones dan 400, así que lo que las distingue es cuántos trozos se llegaron a pedir, y eso es lo que el test mide. `/settings` es una lectura CON el teléfono de quien mira, porque la propuesta pendiente es por teléfono y sólo hay una viva: pedir un segundo cambio pisa el primero, y mostrarlo es lo que evita que pase sin verse. La función del panel se llama `confirmar_desde_el_panel` y **no** `confirmar_pedido`: ese nombre ya es el del mecanismo del submit en `aprobacion`, que ésta no llama y con el que no es intercambiable. **No decide nada** —no mira solicitudes abiertas, no cierra revisiones, no toma el lock, no verifica quién llama—: eso vive entero en `decisiones.confirmar`. Lo único propio del panel es que el pedido sea de esta empresa, con `_pedido_de_la_empresa`, el MISMO chequeo que usa la lectura, y corre ANTES de decidir nada. **El nombre que viaja aguas abajo sale del DOCUMENTO (`doc["name"]`), no de la URL**: ERPNext resuelve sin mirar mayúsculas y Redis no, así que con el texto crudo el panel armaba `solicitud:so-ord-1` mientras `solicitudes.crear` y el botón de WhatsApp (que termina en `.upper()`) tomaban `solicitud:SO-ORD-1` — dos llaves para el mismo pedido es no tener exclusión mutua entre los dos canales. **La respuesta trae `submitted` además de `ok`**: `ok` dice que la acción salió bien, `submitted` dice que el Sales Order quedó en `docstatus=1`. Con una contraoferta abierta los dos difieren y el panel pintaba una venta que no existe. **Y tiene TRES valores, no dos**: `null` es «no se pudo comprobar» —un Submit puede commitear después de que el cliente HTTP se dio por vencido, y si la relectura tampoco contesta nadie sabe cómo quedó— así que viaja sin `bool()`, que colapsaba lo incierto en «no se emitió». **La escritura corre en `_HILOS_ESCRITURA`, no en `_HILOS`**: un confirm espera hasta 10 s por cada uno de dos locks y después la cadena del submit contra timeouts de 20 y 30 s —minutos—, y compartiendo los cuatro hilos de lectura dejaba `/snapshot`, `/today` y `/queue` en la cola con el panel viéndose muerto. No lleva deadline a propósito: cortar la espera no cancela el submit, sólo informaría un fallo sobre un pedido que SÍ se emitió. Escribir exige `puede_decidir`, así que el token compartido entra a mirar y nunca decide. **Las lecturas corren en el pool DEL PANEL** (`_HILOS`), no en el del proceso: `asyncio.to_thread` comparte executor con `main._run_sync`, y seis lecturas lentas dejaban al webhook de WhatsApp esperando hasta 20 s —503, y Meta reintenta el mensaje del cliente—. `quien()` tiene **TRES** respuestas y `ANONIMO` (`""`) es falsy **y válido**: comparar con `is None`, nunca por verdad. El CORS permite `Content-Type` además de `Authorization` —un `fetch` que manda JSON fallaba el preflight y se veía como «el botón no hace nada»— y trae `max-age`, porque `Authorization` obliga a preflight en CADA lectura. **El mismo origen acepta el ASCENSO de esquema y nunca la bajada**: el navegador manda `Origin` siempre en un POST, y comparar el esquema a secas hacía depender el confirm de que el proxy mandara `X-Forwarded-Proto`; aceptar los dos sentidos dejaba que una página en http sobre este host le hablara al servicio https salteándose `allowed_origin`. Se acepta proceso-en-http + navegador-en-https (el proxy terminando TLS) y nada más. `puede_decidir()` es la guarda que cualquier escritura futura tiene que usar; `authorized()` NO sirve para eso —devuelve True para el token compartido—. `conversation` llega **cliente → `mobile_no` → `_thread_tag` → hilo**, la ÚNICA dirección posible: el hilo se guarda bajo `sha256(teléfono)` y no se puede invertir, así que «listar las conversaciones» no existe y no debe construirse. El tag se **importa** de `app/main.py`, nunca se reescribe: dos shas que nadie obliga a coincidir se rompen devolviendo vacío, que no falla en ninguna parte. Un `ToolMessage` se vuelve una línea neutra y su contenido **no sale**; un `AIMessage` vacío no se muestra. Ningún teléfono aparece en ninguna respuesta. `entradas_de_tokens` devuelve las válidas **y los motivos** de lo que descartó: el descarte era mudo, así que un token pegado en el `.env` que no anda no tenía dónde explicarse. `_tokens_por_persona` es esa función con los motivos tirados. **Un teléfono, un token, y la ambigüedad se rechaza ENTERA**: si dos entradas comparten el teléfono normalizado —o el token— caen las dos, no sólo la segunda. Ganaba la primera, y eso no protegía nada: revocar es sacar una línea del `.env`, las dos líneas son indistinguibles ahí, y borrar cualquiera de las dos dejaba a la persona adentro o ascendía un token inerte. El token repetido se comprobaba ANTES que el teléfono, así que dos entradas con el mismo token y teléfonos distintos resolvían al primero: quien recibiera ese token entraba con la identidad del otro, y con sus permisos. `_tokens_por_persona` **loguea los motivos** (una vez por valor, sin el token): tirarlos era lo que hacía invisible todo esto en el proceso que está sirviendo el panel. **`normalizar_token` es la única regla de cómo se compara un token** y la llaman los tres lados (por persona, compartido y `readiness`): había tres recortes distintos y el compartido se comparaba crudo. `Customer` es un maestro **global** en ERPNext: la pertenencia a la empresa se comprueba contra sus pedidos, igual que en `snapshot`. «Hoy» compara el día **LOCAL** del sello (`_dia_local`, vía `reloj.zona()`), no su texto: el checkpoint se sella en UTC, así que comparar los textos borraba de la pantalla justo la franja en que el almacén cierra la caja —la noche al oeste de Greenwich, la madrugada al este—. Las dos mitades del filtro se prueban por separado: que la charla del borde **entre** y que la de ayer **no**. **`sales` es la ÚNICA lectura con parámetro**: `?days=` sale del selector 7/30 y se lee de `scope["query_string"]` —ASGI no la mete en `path`, así que el ruteo por `readers` la encuentra igual— y se acota a 1..90 en `dias_pedidos`, porque un entero sin techo del lado del cliente es pedirle a ERPNext que recorra todo. Si el backend lo ignorara, la pantalla diría «últimos 7 días» sobre números de 30 sin que nada falle. No reusa `gerencia._ventas_del_periodo`: esa herramienta devuelve PROSA para el modelo y de una frase no sale un gráfico. Los renglones se piden con **`parent="Sales Order"`, el DOCTYPE padre**, que en una tabla hija no es opcional —el bug por el que `informe(que="stock_bajo")` nunca contestó contra un ERPNext real—. `averageOrder` sin pedidos es **None y no 0**. **`advice` informa `consejos.activo()` y no lo obedece**: ese interruptor apaga el ENVÍO (Meta cobra por mensaje de servicio desde el 1/10/2026), y leer en un panel que el dueño abrió él mismo no manda nada; filtrar acá dejaría la pantalla vacía justo en la configuración de fábrica. **`peso` no sale al cable** —plata en `perdida`, unidades en `quiebre`, no se comparan— así que se ordena del lado del servidor DENTRO de cada clase y viaja el orden; el panel lo respeta y no re-ordena. `sobre` sale duplicado a propósito: `about` es el texto y `customerId`/`orderId`/`productId` el enlace, exactamente uno. **Lo que no se pudo leer viaja como `null`, nunca como vacío**: una moneda en `""` o un ranking en `[]` pasan por el validador como un VALOR —«no hay moneda», «no se vendió nada»— y en el caso de la moneda lo rechazan entero, llevándose puesto el `errors` que explicaba la falla. Misma regla que `averageOrder`. `topProducts` distingue TRES estados y no dos: leído (lista), no leído (`null` + `errors`) y recortado (`truncated`), y el recorte se mide con un centinela —se piden `LIMIT*8+1` filas— en vez de suponerse. La consulta de ventas lleva tope superior de fecha además del inferior: ERPNext deja fechar un Sales Order adelante, y sin eso entra en los totales y arma una fila de `daily` posterior a `until`, que el panel rechaza como incoherente. |
| `app/digest.py` | 475 | El resumen de las 18:00. Hilo propio, gateado por `DIGEST_ACTIVO`. | No migrar. |
| `app/mcp_cliente.py` | 641 | **La dirección contraria a `mcp_server.py`**: las herramientas de OTRO servidor MCP (hoy `@casys/mcp-erpnext`, 125 de ERPNext) adentro del agente de gerencia. `MCP_EXTERNOS` vacío = no-op. | **`TOOLS_GERENCIA` no se toca**: lo externo va a `TOOLS_AGENTE_GERENCIA` y sólo eso arma el agente — si se sumara a la constante, `mcp_server` la lee y pasaríamos a republicar el `erpnext_doc_submit` de un tercero por NUESTRA puerta autenticada, con el test de catálogo en verde porque los dos lados se mueven juntos. Filtra por NOMBRE (`MCP_EXTERNOS_BLOQUEAR`, con comodines) y no por categoría, porque `--categories` es lo único que trae Casys y el submit vive en DOS categorías: sacarlas se lleva también la de crear el borrador. **El default de `.env.example` NO está vacío**: saca los módulos que este negocio no usa —RRHH, nómina, activos, manufactura, proyectos—, que son un tercio de las 125 y viajan en el prompt de cada turno (149 -> 108 herramientas, ~38.200 -> ~29.100 tokens). No saca ni un poder, y `test_el_bloqueo_por_defecto_saca_modulos_enteros_y_ningun_poder` es lo que impide que alguien le agregue `*_submit` de paso. **`cargar()` cachea por proceso** (`_cargadas`): medir dos configuraciones distintas en el mismo intérprete devuelve la primera las dos veces — pasó midiendo justamente esto. **No usa el SDK `mcp`**, y no por gusto: el modo HTTP de Casys 3.0.4 exige el protocolo `2026-07-28` y le contesta 400 al SDK de Python. Habla el protocolo a mano sobre el `httpx` que ya estaba —`MCP-Protocol-Version`, `Mcp-Method` en toda petición, `Mcp-Name` sólo en `tools/call`, y `protocolVersion` también adentro de `params._meta`—, y manda el SUPERCONJUNTO en vez de negociar: un servidor viejo ignora los headers y las claves de `_meta` que no conoce. Después del `initialize` vale la versión que el servidor DIJO, no la que se pidió. Con eso se fue el bucle de eventos en un hilo de fondo (httpx es síncrono, igual que todo el runtime) y 12 paquetes de la imagen. `_envolver` atrapa `Exception` y NO `MCPExternoError`: un `httpx.ConnectError` —el contenedor caído, o sea el caso normal— que se escape de una herramienta deja un AIMessage sin su ToolMessage y rompe ese hilo para siempre. `args_schema` de estas herramientas es un DICT, no un modelo de pydantic: quien lea un esquema tiene que aguantar las dos formas. **Nunca en TOOLS_CLIENTES**: `erpnext_sales_order_create` pide `items[].rate`, o sea el precio lo pone el modelo, y `policy._precio_autorizado` queda fuera del camino.  **Por stdio se lee en BINARIO y las líneas se arman acá**: `select()` prueba que hay UN byte, no una línea, así que un `readline()` sobre media línea colgaba el cliente para siempre —y con `_candado` tomado, o sea todo lo que viniera después para ese servidor—. La fecha límite se calcula UNA vez y vale para todos los pedazos; `_pendiente` sobrevive a la llamada porque un aviso y la respuesta pueden llegar en la misma escritura. `servidores(env)` lee el mapa que le pasa readiness —y `token_de` y `http_internos` también, porque lee tres variables y abandonar el parámetro en la segunda es el mismo error una línea abajo—. Un token NUNCA sale por `http://` salvo que el destino no llegue a ninguna red (`_destino_sin_red`) o que el servidor esté nombrado en `MCP_EXTERNOS_HTTP_INTERNOS`; se comprueba en `servidores()` Y en `_cabeceras()`, porque un `ClienteMCP` se puede construir sin pasar por el parser. |
| `app/mcp_server.py` | 557 | **El agente de gerencia hablado en Model Context Protocol**: n8n, Claude Code o cualquier harness se conecta y pasa a tener sus herramientas. Dos transportes: `POST /mcp` (montado en `main.py`) y stdio (`python -m app.mcp_server`). | **No hay un segundo registro**: `_catalogo()` lee `graph.TOOLS_GERENCIA`, así que sacar una herramienta del agente la saca de acá en el mismo commit. Publica `tool_call_schema` y NO `args_schema` — aunque hoy los dos excluyen el `RunnableConfig` inyectado (la mutación lo desmintió), lo que se protege es que ninguna herramienta publique un parámetro que diga quién sos: la identidad sale del token. **Un token ES un teléfono** (`MCP_TOKENS`, mismo parser que el panel) y NO hay token anónimo, al revés que el panel: `require_management` compara ese número contra `TELEFONOS_EQUIPO`, así que un token de alguien de afuera se autentica y no lee nada. Sin `MCP_TOKENS` el endpoint es 503 — configurar la autenticación ES encenderlo. Un fallo de herramienta vuelve como `isError` y nunca como excepción JSON-RPC, por lo mismo que `graph.handle_tool_errors`. En stdio, **stdout es el protocolo**, y que este archivo no tenga un `print` a secas NO alcanza: `_catalogo()` importa `app.graph` TARDE, recién en el `tools/list`, y esa importación arrastra medio proyecto —~370 `print()` escritos para el log del contenedor del agente, donde stdout está bien—. Medido: un `tools/list` sacaba «[limites] no pude leer el almacén» como PRIMERA línea de stdout, o sea que un módulo que no sabe que MCP existe rompía el servidor MCP. Por eso **`_canal_apartado()`**: mientras corre `main()`, `sys.stdout` ES stderr y las respuestas se escriben al descriptor apartado, al que nadie más tiene referencia. Cualquier `print` futuro de cualquier módulo nace del lado correcto; arreglar los 370 llamados no era el arreglo. El `title` de `initialize` —el único campo del protocolo que una persona lee con los ojos— sale de `NOMBRE_NEGOCIO` (`server_title()`), no de una constante: era el nombre de UN cliente en un archivo. |
| `app/reloj.py` | 181 | **El reloj del negocio, uno solo.** | `zona()`/`ahora()`/`hoy()` **levantan** y son para código que DECIDE. `zona_con_respaldo(quien)`/`ahora_con_respaldo` no levantan y son para el que **informa, barre o sella** — un barrido usa éstas. Los sellos sin zona de ERPNext se leen **sólo** con `de_erpnext()`. El literal `"America/Argentina/Buenos_Aires"` no puede aparecer en ningún otro archivo de `app/`. |
| `app/tools/entrega.py` | 251 | **Las condiciones de entrega, del lado del CLIENTE.** UNA herramienta de sólo lectura (`condiciones_de_entrega`) que contesta las seis preguntas de mostrador —qué días y a qué hora sale el reparto, a qué zonas se llega, si se puede pasar a buscar el pedido, cuánto sale una entrega fuera de día y desde qué total— con los doce ajustes del grupo `ENTREGA` de `app/limites.py`, que hasta ahora no leía **ninguna** herramienta del agente de clientes. | Todo sale de `limites.vigente(...)` y NUNCA de `os.getenv`: `vigente()` es lo que mueve el WhatsApp del dueño, y devuelve `NINGUNO` cuando las reglas de entrega se PERDIERON —el estado en que `limites.entrega()` no ofrece nada—, así que leer el entorno acá le contaría al cliente una ronda que el sistema no va a hacer. `_valor` colapsa a `""` TRES estados (nunca se configuró, se perdió el almacén, está guardado y no se puede interpretar) porque para el cliente son el mismo, y **ninguno de los tres es un «no»**: quien impide que «no lo tengo configurado» se lea como «no repartimos» es la última línea de la respuesta (`condiciones.instruccion`), en el mismo lugar y con la misma función que el «No confirmes disponibilidad.» de `consultar_stock`. Un booleano tiene **tres** estados acá, así que el default del dict es `""` y no «no»: con `RETIRO_LOCAL_ACTIVO` el `false` es del registro y se contesta, el vacío es el almacén perdido y no se contesta. Con la excepción de entrega APAGADA no se imprimen el cargo ni el mínimo — cotizarlos sería un precio que nadie autorizó. `zona` es un PARÁMETRO y no una lista que lee el modelo: la comparación la hacen `entrega.normalizar_localidad`/`normalizar_cp`, las mismas que deciden la entrega de verdad, así que lo que escucha el cliente no puede discrepar con lo que hace el sistema; y son **cuatro** respuestas, porque un código postal no se busca en una lista de localidades (ahí falta el dato, no es un «no»). Es la única llamadora de `entrega.fuera_de_zona`, que estaba escrita y sin usar, y nunca la manda sola. NUNCA en `TOOLS_GERENCIA`: el dueño ya lee y CAMBIA estos doce con `ver_ajustes` y `proponer_limite`, y una segunda puerta a lo mismo es el solapamiento que degrada la elección del modelo. |
| `app/pais.py` + `app/telefono.py` + `app/formato.py` | 120 + 180 + ~90 | **UN solo ajuste de país (`PAIS_NEGOCIO`, ISO de dos letras) del que salen el código de discado, el locale y la moneda**, preguntándole a libphonenumber y a CLDR. `AR -> +54 es_AR ARS`, `US -> +1 en_US USD`. | Las viejas GANAN: `PAIS_TELEFONO` y `LOCALE` puestas a mano mandan sobre lo derivado, para que un despliegue que ya existe no cambie de conducta al actualizar — **y por eso `.env.example` NO puede traer `PAIS_TELEFONO=54` escrito**, o una instalación en Estados Unidos que cambia lo único que la plantilla dice que hay que cambiar sigue normalizando como argentina y los clientes cargados dejan de matchear; `readiness` avisa cuando las dos se contradicen, que es lo único que alcanza a los `.env` que ya existen. `codigo_de`/`locale_de` (territorio explícito) están separadas de `codigo_telefono`/`locale` (las del entorno, que terminan en el default) porque son dos preguntas: «qué uso» siempre tiene respuesta y «esto existe» tiene que poder decir que no. `telefono.PAIS`/`REGION` se congelan al importar, así que quien mira un `.env` CANDIDATO pasa `normalizar(raw, pais)`. `_PREFIJO_MOVIL = {54: "9"}` es lo que la librería no puede saber —WhatsApp siempre es móvil— y es una tabla por país, no un `if` sobre Argentina. El paso 2 de `normalizar` (nacional VÁLIDO de la región, antes de la heurística de E.164-sin-`+`) existe por Brasil: un móvil nacional brasileño son once dígitos sin 0, exactamente lo que el paso 3 se llevaba puesto. **NO es un ajuste del panel**: el código de país decide QUIÉN es un teléfono, y cambiarlo con clientes cargados no reescribe lo que está guardado en ERPNext. |

---

## Los tests: cómo está armado el harness

- `tests/conftest.py` es **el único conftest**. Estampa un entorno falso *antes*
  de importar `app`, y deja **sin fijar** `BUSINESS_TIMEZONE`, `IDIOMA_*` y
  `LOCALE` — porque CI corre **cinco celdas** con esos valores cambiados.
- **Cinco fixtures, todas autouse:** `limites_sin_redis`, `marcas_sin_redis`,
  `_idioma_declarado`, `_locale_declarado`, `el_barrido_no_lee_el_reloj_real`.
- `RelojDePrueba("2026-09-08")`: un archivo **nombra su momento** y arma las
  horas desde ahí (`a_las`, `epoch`, `sello`, `sello_partido`), resolviendo la
  zona en el momento de la llamada. Por eso los mismos tests pasan en Buenos
  Aires y en Kolkata.
- `tests/fakes.py::listar` es el doble de consulta de ERPNext y **honra**
  filtros, `order_by`, `limit` y `start`. Es a propósito: un doble que ignorara
  el orden no podría discrepar con el código sobre «leé el extremo más nuevo».
- `el_barrido_no_lee_el_reloj_real` hace que `pendientes._ahora()` llame a
  `pytest.fail` **en toda la suite**. Usa `pytest.fail` y no `assert` porque
  `Failed` hereda de `BaseException` y un `except Exception` de producción no
  puede tragárselo.
- Un módulo que agrega variables de entorno las agrega al `delenv` de
  `limites_sin_redis`, o el `.env` de una máquina de desarrollo configura los
  tests.
- `inventario_confiable` (el doble de conftest) **honra el interruptor que le
  fijan**. Era un `lambda` que devolvía lo mismo pase lo que pase, así que
  descartaba el `STOCK_CONFIABLE` de la línea de arriba: con ése puesto, un
  test de `test_sombra.py` afirmaba lo CONTRARIO de lo que hacía producción y
  pasaba igual. Cuarta aparición del defecto de más arriba.

---

## La superficie de herramientas: qué ve cada agente

`app/graph.py` arma DOS registros, y cuál se monta en qué agente es un límite de
privilegio, no un detalle de cableado. Hoy: **12 de clientes, 25 en el registro de
gerencia — de las cuales 4 son compartidas, así que 21 son sólo de
gerencia** (eran 19 sólo-de-gerencia antes de colapsarlas, y 23 antes de eso).
El número de gerencia no se cuenta a mano: sale de
`tests/test_autorizacion_gerencia.py`, que deriva `SOLO_GERENCIA` de
`TOOLS_GERENCIA − TOOLS_CLIENTES` y afirma 21. Decía 24 y 20 acá hasta el
16/09: el test estaba al día y el mapa no, que es la forma en que un mapa hace
daño — se le cree.

**Y ese registro NO es lo que se le monta al agente.** `TOOLS_AGENTE_GERENCIA =
_con_externas()` le suma las herramientas de los servidores MCP de
`MCP_EXTERNOS`, que no están en este repo y se descubren en el import. Medido
en el VM el 16/09 con `@casys/mcp-erpnext`: 84 externas sobre las 25 propias,
109 en total, ~29.100 tokens de prompt por turno. Lo que se publica por
`app/mcp_server.py` sigue siendo `TOOLS_GERENCIA` a secas, y esa distinción es
a propósito: republicar la superficie de un tercero por nuestra puerta sería lo
peor de las dos, porque parece nuestra. **Un servidor externo caído ya no es
mudo**: `_con_externas` imprime el resumen —con el motivo— también cuando hay
servidor configurado y no entró ninguna herramienta; antes no dejaba una sola
línea y se leía igual que «nadie configuró nada». La de clientes subió a 12 con
`condiciones_de_entrega`.

Y desde `app/mcp_server.py` esas 19 son también la superficie MCP, sin una
segunda lista: lo que se saca de `TOOLS_GERENCIA` deja de existir para n8n y
para Claude Code en el mismo commit.

**La regla, escrita una sola vez y con un motivo medido:** detrás de un
`Literal` van **LECTURAS de un mismo tema, nunca una escritura**. Consolidar por
subdivisión natural de tareas es lo que recomienda la guía de Anthropic;
`manage_x(action=…)` —todo detrás de un modo— lo desaconseja explícitamente,
porque obliga al modelo a resolver el modo antes que la tarea.

| herramienta | ramas | por qué se colapsó |
|---|---|---|
| `informe(que=…)` | pendientes, ventas, stock_bajo, cobranzas, autonomia | `cobranzas_vencidas` era `run_report("Accounts Receivable")` con un filtro, o sea una de las siete consultas que `ejecutar_reporte` ya corría: dos herramientas plausibles para «¿cuánto me deben?» |
| `ver_ajustes(que=…)` | limites, entrega, historial | tres lecturas del mismo tema («qué tengo configurado») con la misma guarda y la misma negativa |

**Lo que NO se colapsa, y no por gusto:**
- `proponer_limite` y `proponer_accion` escriben (proponen). Además el router de
  `app/main.py` distingue sus confirmaciones **por el largo del código** —cuatro
  dígitos un ajuste, seis un pedido— así que fusionarlas es fusionar dos caminos
  de confirmación.
- `estado_del_sistema` y `ver_avisos_fallidos` siguen separadas: ahí el
  solapamiento estaba en el TEXTO (la primera decía «usala si pregunta si los
  avisos están saliendo», que es la otra), y se arregló el texto.

**Lo que degrada la elección es el solapamiento, no la cantidad.** El tamaño del
catálogo pesa recién mucho más lejos (RAG-MCP arXiv:2505.03275 mide sobre
catálogos de cientos; LongFuncEval arXiv:2505.10570 escalando a 120K tokens de
catálogo). Lo que sí toca a una lista corta: presentar cinco candidatos donde
alcanzan dos cuesta ~6 puntos de acierto (arXiv:2605.24660).

**La trampa que trajo el colapso.** Con enums, el modelo puede elegir bien la
herramienta y escribir mal el valor —y es la falla probable: Gemini ignora
`strict` en su capa compatible con OpenAI, así que el `Literal` es una
SUGERENCIA y la única validación real es la de pydantic; y con herramientas en
castellano la falla medida más común es que el modelo escriba el valor en el
idioma del usuario (arXiv:2601.05366). Por eso `graph._error_de_herramienta`
existe: un `literal_error` vuelve con los valores válidos y el modelo reintenta;
cualquier otra excepción sigue yendo a `_ERROR_MSG`, que manda a
`escalar_a_humano`. Antes, un guión bajo mal puesto mandaba al dueño a esperar
a una persona.

**El idioma, por módulo.** YA NO HAY DIFERENCIA: los siete módulos de
gerencia —`operaciones`, `gerencia`, `crm`, `configuracion`, `gestion`,
`captura` y `memoria`— devuelven todo por `idioma.t(...)` con
`idioma.gerencia()` como lengua, y
`tests/test_idioma_cobertura.py::test_ninguna_herramienta_de_gerencia_tiene_castellano_escrito_a_mano`
es lo que impide que el octavo se escriba a mano. Lo que SÍ queda: un motivo
que viaja como PARÁMETRO no se traduce al interpolarse. `LimiteError` ya lo
resolvió (lleva `clave` y `datos`, y `limites.motivo(exc, lengua)` los usa;
`resumen(lengua)` es lo que lo conecta con `ver_ajustes`). `AccionError`
—`app/acciones.py`, 28 `raise`— ahora también: lleva `clave` y `datos`, y el
motivo se arma cuando se LEE. El primitivo es `idioma.motivo_de(exc, lengua)`,
uno solo para los dos (`limites.motivo` delega en él). **Un dato puede ser un
`lambda lengua:`**, y eso es lo que `limites.motivo` no tenía: casi todos los
datos valen igual en los dos idiomas —un número de pedido, un monto—, pero
«falta el día y la hora» es prosa, y quien levanta la excepción no sabe quién la
va a leer. El `str(exc)` en castellano sigue intacto: es lo que va al log.

---

## Las trampas que ya costaron tiempo

1. **`erpnext.add_comment` se traga los errores; `registrar_comentario` los
   levanta.** `marcas.escribir(exigir=True)` usa el segundo. Para algo de lo que
   depende una promesa a un cliente, «no hubo excepción» tiene que seguir
   significando «quedó escrito».
2. **El `content` de un comentario vuelve envuelto en HTML y con entidades
   escapadas.** El doble de test **no** lo envuelve, así que un parseo hecho a
   mano pasa todos los tests y falla en producción. Por eso existe
   `marcas.texto_plano` y por eso `tests/test_pendientes.py:329` simula el
   destrozo.
3. **El JSON de un marcador está anclado al FINAL del comentario.** Cualquier
   texto después de la carga lo vuelve ilegible — y se saltea en silencio, no da
   error.
4. **`delivery_date` es un `Date` de ERPNext: `"YYYY-MM-DD"` pelado, sin hora y
   sin zona.** La hora de entrega es **configuración del dueño**
   (`ENTREGA_HORA`, vía `excepciones.hora_reparto()`). Todo lo que necesite una
   hora del día tiene que traérsela; el pedido no la tiene.
5. **La fecha de entrega no es final en la creación**: `policy_aplicar_terminos`
   la reescribe cuando el cliente acepta una contraoferta. Lo que dependa de
   ella **se re-lee a la hora de actuar**.
6. **Un aviso que dispara un barrido sale casi siempre fuera de la ventana de
   24 h de Meta.** Sin `plantilla_env` no falla a veces: falla **siempre**, se
   gasta los 8 reintentos y muere en la cola de descarte, en silencio.
7. **Los parámetros de una plantilla son DATOS** (número de pedido, fecha,
   monto), nunca prosa: la plantilla está registrada en **un** idioma y una
   frase traducida adentro de un parámetro no traduce nada.
8. **`policy_get_list` con la clave de cliente devuelve lo que ese usuario ve.**
   Un hilo de fondo no tiene scope: siempre `policy_get_*`.
9. **`docker compose restart` NO re-lee `.env`** — hace falta
   `up -d --force-recreate`.
10. **`LLM_PROVIDER` en blanco significa `qwen`, no gemini.** No hay fallback
    entre proveedores, por diseño.
11. **`locks.conexion()` no es el redis que usan los locks.** `limites_sin_redis`
    (autouse) le pone un `FakeRedis` a `conexion` para que el almacén de límites
    arranque vacío, pero `distributed_lock` toma `_redis()` directo. Un test que
    manipule la llave de un lock por `conexion()` escribe en el doble, el lock de
    verdad queda intacto y el test pasa sin haber ejercido nada.
